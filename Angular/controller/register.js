routerApp.controller('registercontroller', ['$scope',function($scope) {

		$scope.register = function validation(user){

   var emailRegex = /^[A-Za-z0-9._]*\@[A-Za-z]*\.[A-Za-z]{2,5}$/;

 if(user.username = ""){
 alert(user.username);
	}
}
	
	}]);